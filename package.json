{
  "devDependencies": {
    "cheerio": "^1.1.2",
    "esbuild": "^0.25.9",
    "html-minifier-terser": "^7.2.0",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "build:prod": "node build.mjs && node -e \"const {exec}=require('child_process'); const p=process.platform; const opener=(p==='win32'?'start ':p==='darwin'?'open ':'xdg-open '); exec(opener + 'http://localhost:63342/longshotbyfar/dist/index.html')\""
  }
}
